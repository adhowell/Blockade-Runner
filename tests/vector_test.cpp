#include <gtest/gtest.h>

//#include "../physics/include/vector.h"

// Demonstrate some basic assertions.
TEST(VectorTest, VectorAddition) {
    //auto v1 = Vector(1, 5);
    //auto v2 = Vector(2, -2);
    // Expect two strings not to be equal.
    //EXPECT_EQ(v1+v2, Vector(3, 3));
    EXPECT_EQ(1, 1);
}
